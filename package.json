{
  "name": "tetris",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "watch:scss": "npx node-sass -w src/sass/style.scss -o src/styles/ --output-style=compressed"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "node-sass": "^8.0.0",
    "vite": "^4.1.0"
  },
  "dependencies": {
    "vite-plugin-minify": "^1.5.2"
  }
}
